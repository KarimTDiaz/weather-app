import styled from 'styled-components';
import { COLORS } from '../../constants/variables';

const StyledOverviewWeatherContainer = styled.div`
	padding: 3rem 1rem;
	background-color: ${COLORS.dark};
`;

export { StyledOverviewWeatherContainer };

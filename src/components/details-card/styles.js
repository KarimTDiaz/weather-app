import styled from 'styled-components';

const DetailsCardContainer = styled.div`
	position: relative;
	z-index: 1;
`;

const DetailsCardData = styled.div`
	padding: 1rem;
	text-align: center;
`;

export { DetailsCardContainer, DetailsCardData };
